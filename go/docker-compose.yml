version: "3"
services:
  app:
    image: cmsn-scrap-go:latest
    container_name: cmsn-scrap-go
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
