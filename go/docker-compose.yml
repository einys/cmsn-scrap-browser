version: "3"
services:
  app:
    image: cmsn-scrap-go:latest
    container_name: cmsn-scrap-go
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18081:8080"
    volumes:
      - ./app:/app
